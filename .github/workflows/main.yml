name: Debug Geocode

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Say hello (prove parsing + run button)
        run: echo "✅ Actions is running and this workflow is parsed"
